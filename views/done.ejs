<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Processing...</title>
    <link rel="stylesheet" href="assets/style.css">
    <style>
        .progress {
            height: 10px; 
            width: 100%; /* Represents 2 out of 3 steps completed */
            background-color: #4b7f52; 
            border-radius: 5px; 
        }
       
    </style>
</head>
<body>
    <div class="left">
        <img src="<%= data %>" alt="QR Code"> <!-- Replace with your QR code image path -->
    </div>
    <div class="right">
        <div class="progress-bar">
            <div class="progress"></div>
        </div>
        <h1>Processing...</h1> 
        <% if(error){ %>
            <button class="action-button" class="retry-process" onclick="retry();">Retry</button>
            <a class="action-button" href="/">Exit</a>
            <h4> <%= error%></h4>
            <script>
                function retry(){
                    document.getElementById('autoload').submit();
                }
            </script>
            <% } %>
        <form method="POST" id="autoload">
        </form>
    </div>
    <% if(!error){ %>
        <script>
            document.addEventListener('DOMContentLoaded', function () {
                    document.getElementById('autoload').submit();
                });
        </script>
        <% } %>
</body>
</html>